<h1>Songs</h1>
<h1>Click a Song To View</h1>
<%= link_to 'Create a new song', new_artist_song_path(@artist) %>
<%= link_to 'Back to Artists', artists_path(@artist)%>
<%# <%= link_to 'Billboard 100', song_billboards_path(@billboard) %> 
<ul>
  <% @songs.each do |song| %>
    <li><%= link_to song.name, [@artist, song] %></li>
  <% end %>
</ul>